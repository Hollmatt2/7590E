# Threshold note

One to two pages (brief, section 6.4). Graded on the reasoning, not on the threshold chosen. Matt writes it
once the AI step has results.

Evidence to draw on: the per-category results in `docs/evaluation-*.md`, the modified-agreement results, and
the client's warning in section 1 ("If it misses something real, people stop trusting it").

## Where did you set the confidence threshold for surfacing a flag, and why?

## Which is worse for Calder: a flag that wastes a reviewer's time, or a real problem the system stayed silent about?

## What does your threshold mean in practice? Roughly how much does the system miss?

## What would you tell the Associate General Counsel if she asked whether she can rely on it?
